package com.activwork.etms;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/**
 * Integration tests for the ETMS Spring Boot Application.
 * 
 * <p>This test class verifies that the application context loads successfully
 * and that the database connection is properly configured.
 */
@SpringBootTest
class EmploymentTrainingManagementSystemApplicationTests {

    /**
     * Test that the application context loads successfully.
     * This also verifies database connectivity.
     */
    @Test
    void contextLoads() {
        // If this test passes, it means:
        // 1. Spring Boot application context loaded successfully
        // 2. Database connection is working
        // 3. All beans are configured correctly
    }

}
